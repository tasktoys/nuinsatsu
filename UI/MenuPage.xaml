<Page x:Class="NUInsatsu.UI.MenuPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="350" d:DesignWidth="515"
      Title="MenuPage" xmlns:my="clr-namespace:NUInsatsu.UI">


    <StackPanel>
        <my:PageHeader x:Name="pageHeader1" VerticalAlignment="Top" Title="項目を選択してください" />
        <Button Name="entryButton" Content="新規ドキュメントの登録" Height="88" Margin="10" VerticalAlignment="Top" Click="entryButton_Click" />
        <Button Name="printButton" Content="登録ドキュメントの印刷" Height="83"  Margin="10" VerticalAlignment="Top" Click="printButton_Click" />

        <DockPanel Style="{StaticResource Bottom}" >
            <Button Name="adminButton" Content="管理メニュー" Style="{StaticResource NextButton}" />
        </DockPanel>
        
    </StackPanel>

</Page>
